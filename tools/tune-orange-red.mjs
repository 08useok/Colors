import {applyBeta6Balance} from '../src/config/beta6-balance.js';import{beta6Duel}from'./simulate-beta6-balance.mjs';const ss=[10,13,16].flatMap(distance=>[.035,.1].map(aimError=>({distance,aimError})));function wins(c,l,r,n=10){let w=0;for(let s=0;s<ss.length;s++)for(let i=0;i<n;i++)for(const m of[1,-1]){const x=m===1?beta6Duel(c,l,r,ss[s],20260919+s*10000+i,1):beta6Duel(c,r,l,ss[s],20260919+s*10000+i,-1);if(x.outcome*m>0)w++;}return w;}const rows=[];for(const health of[4400,5800,7000,8500,10000])for(const splash of[700,1000,1300,1600,2000,2500]){const c=applyBeta6Balance();Object.assign(c.orange,{maxHealth:health,bombSplashDamage:splash});rows.push({health,splash,orangeVsRed:wins(c,'orange','red'),purpleVsOrange:wins(c,'purple','orange')});}console.table(rows.sort((a,b)=>b.orangeVsRed-a.orangeVsRed||b.purpleVsOrange-a.purpleVsOrange||a.health-b.health||a.splash-b.splash));
