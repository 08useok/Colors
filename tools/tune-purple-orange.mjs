import{applyBeta6Balance}from'../src/config/beta6-balance.js';import{beta6Duel}from'./simulate-beta6-balance.mjs';const ss=[10,13,16].flatMap(distance=>[.035,.1].map(aimError=>({distance,aimError})));function wins(c,l,r,n=50){let w=0;for(let s=0;s<ss.length;s++)for(let i=0;i<n;i++)for(const m of[1,-1]){const x=m===1?beta6Duel(c,l,r,ss[s],20260919+s*10000+i,1):beta6Duel(c,r,l,ss[s],20260919+s*10000+i,-1);if(x.outcome*m>0)w++;}return w;}const rows=[];for(const needle of[1010,1020,1040,1060,1080,1100])for(const poison of[912]){const c=applyBeta6Balance();Object.assign(c.purple,{needleDamage:needle,poisonDPS:poison});rows.push({needle,poison,purpleVsOrange:wins(c,'purple','orange'),pinkVsPurple:wins(c,'pink','purple')});}console.table(rows.filter(x=>x.purpleVsOrange===600&&x.pinkVsPurple===600).sort((a,b)=>a.needle-b.needle||a.poison-b.poison));
