import{applyBeta6Balance}from'../src/config/beta6-balance.js';import{beta6Duel}from'./simulate-beta6-balance.mjs';const ss=[10,13,16].flatMap(distance=>[.035,.1].map(aimError=>({distance,aimError})));function wins(c,l,r,n=50){let w=0;for(let s=0;s<ss.length;s++)for(let i=0;i<n;i++)for(const m of[1,-1]){const x=m===1?beta6Duel(c,l,r,ss[s],20260919+s*10000+i,1):beta6Duel(c,r,l,ss[s],20260919+s*10000+i,-1);if(x.outcome*m>0)w++;}return w;}const rows=[];for(const health of[9000,9500,10000])for(const charge of[3])for(const length of[4]){const c=applyBeta6Balance();Object.assign(c.azure,{maxHealth:health,surfLength:length});Object.assign(c.azure.ultimate,{chargeRequired:charge,damage:4800,range:6});rows.push({health,charge,length,azureVsCrimson:wins(c,'azure','crimson'),greenVsAzure:wins(c,'green','azure')});}console.table(rows.filter(x=>x.azureVsCrimson===600&&x.greenVsAzure===600).sort((a,b)=>a.health-b.health||b.charge-a.charge||a.length-b.length));
